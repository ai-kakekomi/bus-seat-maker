# bus-seat-maker 開発の掟

添乗員向けバス座席表ジェネレータ。ai-kakekomi.com/apps/bus-seat/ で配信（LPにsubmodule組込）。
実物（H.I.S.手書き座席表）と、ゆみさん（阪急交通社の添乗現場）の回答との突き合わせで仕様を固めた。
核心は2段階（性別無視の配置パズル→グループ内スワップで男女調整）と「泣き別れ＝最悪エラー」の優先順位。

## 配置エンジン
- **エンジンは2つある**。既定は**テトリス方式（逐次）** `tetrisAssignDay()`。旧「採点＋探索」`buildBestDay()` は `assign({ engine: 'legacy' })` で残置（比較用）
- **旧エンジンに逐次ルールを採点項目として足すのは3回試して失敗した**。既存の重みと綱引きになり収束しない。書き直すなら別関数にすること
- テトリス方式の手順：①申し込み順（2日目は逆順）に理想のかたちのまま前から落とす ②全員入れば終わり（採点も探索もしない）③あふれたら順番を入れ替えて端数を打ち消す ④それでも入らなければかたちを変える（3名→L字、5名→横一列4＋1ほか `tetrisShapeVariants()`）⑤悪くならない範囲で変え続ける。「ふつうに詰める」と「前の列にすき間を残さない（tight）」の両方を最後まで通して良いほうを採る
- **置き場を決める規則は1本**：「本来のかたちを保てるなら、より前の列」（列が外側、かたちが内側のループ）。前後の寄せ方を区別しないので、5名は最前列2席＋次の列3席に崩さず入り、3名・4名（横一列）は入らないので最前列を空けたまま次の列へ送る。この2つが同じ規則から出る

## 確定仕様（ゆみさん回答＋伊藤判断、すべて実装済み）
1. 相席はオール・オア・ナッシング。全員相席のときは後方を自由席。**空席に余裕があれば相席なし**（`shouldAvoidSharing`。絶対に落とさない）
2. 理想のかたちは `idealShapeOf()`。3名＝通路またぎ横一列／4名＝通路またぎ横一列／5名＝正方形＋通路またぎ1／6名＝横一列4＋2／7名以上＝横4席の列を重ねてあまりを次の列（あまり1席なら窓側を空ける）
3. 左右・前後の寄せ方は区別しない
4. 最後部列は最終手段
5. 相席なしの1人客は窓側
6. **2名グループの通路またぎは禁止**（col2-col3。泣き別れ扱い、候補から除外）
7. 2日目は前後反転。**前席オプション組も反転対象**
8. 相席の当番は日ごとに回す
9. 同姓は「下のお名前」入力→`名字＋名前＋様`

## 検収で刺さった罠（再発させない）
1. 見直しの段が申し込み順を崩す → 動かす組は「困っている組」から、出来ばえに順番のずれを加算
2. `relaxGenderConflicts()` が前席オプション組を非オプション組と入れ替えていた（料金をいただいた席を手放す）
3. 前席組だけ「ずらす量」を日ごとにすると、2組のとき前後反転と打ち消し合って1日目と同じ並びに戻る → **ずらすのは通常組と同じく2日ごと（pairIndex）**
4. 後方判定を最後部2列だけにすると「うしろ半分に連日いる」を見逃す → `rearHalfStayCount()` を出来ばえに加算

バグはどれも配置本体ではなく、その後の「見直し」の段にあった。良くしようとする処理ほど、仕様（申し込み順・有料オプション・席替え）を静かに壊す。

## その他
- 端数の勘定は `idealFeasibility()`。最前列が「2席使う列」を、最後部列が「3席＋2席」を吸収する点を勘定に入れる（入れないと見本を誤って不可能と判定）
- 割り切り：かたちが揃わないとき／連日後方になるときは警告で手直しを促す（`shape-differs`・`rear-stay`）。**うしろ半分の連続はゼロにできない**（42名/43席＋前席4組では、残り10組のうち5組ほどが毎日うしろ半分に入るしかない）。2組以下に抑える方針
- 改修フロー：検収（`node test/run.js`＋独自データのスポットチェック）→本体push→LPのsubmodule参照更新push→約2分後に本番curl確認（デプロイが止まったら空コミットで再トリガー）。manual.htmlは308リダイレクトするので `curl -L`
- `assign()` の入力は `days`（dayCountではない）、レイアウトキーは `11x45`/`12x49`
- ゆみさんへの残確認（2026/8/14 LINE送付済み）：9名以上の理想の形／前後反転で真ん中のグループが動かない件
